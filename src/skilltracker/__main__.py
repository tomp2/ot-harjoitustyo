"""Allows starting this app by executing the skilltracker package module.

Start app on command line with modulename with the following command:
`$ python -m skilltracker`
"""
from skilltracker.index import main

if __name__ == "__main__":
    main()
